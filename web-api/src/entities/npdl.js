import { randomUUID } from "node:crypto";

export default class NPDL {
  constructor({ name, age, power }) {
    this.UI = "";
    // this.id = randomUUID();
    // this.name = name;
    // this.age = age;
    // this.power = power;
  }
}
